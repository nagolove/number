reset && gcc -o number-test.exe number-test.c -Wall -std=c2x -lm && ./number-test.exe

# тут проверка того, как происходит возврат структур на x86_64 
# возврат структуры по значению происходит через скрытый указатель. 
# перед вызовом функции вызывающая сторона выделяет память на стеке. 
# вызываемая функция работает через скрытый указатель передаваемый как 
# аргумент вызываемой функции
gcc -O0 -S sret.c -o sret.s -masm=intel
